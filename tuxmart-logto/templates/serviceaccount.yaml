apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tuxmart-logto.serviceAccountName" . }}
  labels:
    {{- include "tuxmart-logto.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
